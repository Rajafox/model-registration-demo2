
{
  "name": "model-registry-frontend",
  "version": "1.0.0",
  "description": "TypeScript frontend for Model Registry",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "compile": "tsc --outDir . --target ES5"
  },
  "devDependencies": {
    "@types/angular": "^1.8.0",
    "typescript": "^4.9.5"
  }
}
